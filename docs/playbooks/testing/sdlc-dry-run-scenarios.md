# Disabled Validation Draft

此文件保留為暫時占位，內容已從目前使用者功能收斂分支中停用。

Validation scenarios 屬於 repo 完成到一定程度後，另開測試分支進行的驗證資產，不屬於目前給使用者日常使用的 SDLC Playbooks。

目前一般 `/sdlc:*` 工作流程不應引用本檔，也不應在 `Playbooks Used` 中列出本檔。

後續若需要重新建立 validation scenarios，應在獨立測試分支設計，並評估是否放入 `docs/validation/` 或其他非使用者功能路徑。
